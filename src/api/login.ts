import { AppThunkAction } from '../thunk';

export function login(username: string, password: string): AppThunkAction<Promise<boolean>> {
  return async function(dispatch, getState) {
    try {
      IpcRenderer
    }
  }
}
